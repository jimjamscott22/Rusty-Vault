//! `rusty_vault ping` — check server connectivity and report latency.

use anyhow::Result;

/// Runs the `ping` command.
pub fn run() -> Result<()> {
    println!("ping: not yet implemented (Phase 4)");
    Ok(())
}
